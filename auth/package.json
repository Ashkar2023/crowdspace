{
    "name": "auth",
    "version": "1.0.0",
    "type": "module",
    "main": "dist/index.js",
    "license": "MIT",
    "scripts": {
        "dev": "nodemon -w src/ --ext ts,cts,mts,json --ignore node_modules --ignore dist --exec \"npm run build && node --env-file=.env dist/index.js\" ",
        "build": "rimraf dist && tsc && tsc-alias"
    },
    "dependencies": {
        "@crowdspace/common": "*",
        "cookie-parser": "^1.4.7",
        "express": "^4.21.1",
        "mongoose": "^8.8.2",
        "morgan": "^1.10.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.7",
        "rimraf": "^6.0.1",
        "tsc-alias": "^1.8.10"
    }
}
